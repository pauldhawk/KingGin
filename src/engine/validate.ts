import type { Hand } from './types';
import type { ContractSpec } from './contracts';

export function validateContract(_hand: Hand, _contract: ContractSpec): boolean {
  throw new Error('not implemented');
}
